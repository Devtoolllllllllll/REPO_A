name: workflow_1
on: workflow_dispatch
jobs:
  code_checkout:
    runs-on: ubuntu-latest
    steps:
      - name: code checkout
        uses: actions/checkout@v4

      - name: printing the html content
        run: cat index.html


 
   

